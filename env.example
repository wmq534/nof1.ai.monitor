# 企业微信机器人webhook地址（可选，如配置则启用企业微信通知）
WECHAT_WEBHOOK_URL=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=YOUR_WEBHOOK_KEY

# Telegram 机器人设置（可选，如配置则启用Telegram通知）
# 需要同时配置 TELEGRAM_BOT_TOKEN 与 TELEGRAM_CHAT_ID
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
# Telegram 代理，默认 127.0.0.1:7890
TELEGRAM_PROXY=127.0.0.1:7890

# 关注的模型列表，多个用逗号分隔，为空则监控所有模型
# 例如：deepseek-chat-v3.1,claude-sonnet-4-5
MONITORED_MODELS=

# API接口地址
API_URL=https://nof1.ai/api

# 是否保存历史数据到data目录，默认为False
SAVE_HISTORY_DATA=False


# ========== Bitget 交易所自动跟单配置 ==========

# ========== 实盘 API 配置 ==========
# Bitget 实盘 API 配置（可选，启用实盘跟单时需要配置）
# 获取方式：登录 Bitget -> API 管理 -> 创建 API
# 注意：请确保 API 权限包含"合约交易"权限
BITGET_API_KEY=
BITGET_SECRET_KEY=
BITGET_PASSPHRASE=

# ========== 模拟盘 API 配置 ==========
# Bitget 模拟盘 API 配置（可选，启用模拟盘跟单时需要配置）
# 获取方式：登录 Bitget -> 模拟交易 -> API 管理 -> 创建模拟盘 API
# 注意：模拟盘需要单独创建 API Key，与实盘 API Key 不同！
BITGET_DEMO_API_KEY=
BITGET_DEMO_SECRET_KEY=
BITGET_DEMO_PASSPHRASE=

# ========== 交易模式选择 ==========
# BITGET_TRADING_MODE: 交易模式选择
#   0 = 只在模拟盘下单（安全测试，推荐新手使用）
#   1 = 只在实盘下单（真实交易，谨慎使用）
#   2 = 模拟盘和实盘同时下单（双重验证模式）
# 默认值：0（模拟盘）
BITGET_TRADING_MODE=0

# Bitget API 环境（一般不需要修改）
BITGET_API_URL=https://api.bitget.com

# Bitget 交易量缩放比例（可选，不设置则使用 bitget_config.json 中的配置）
# 范围：0.001 - 1.0
# 例如：0.01 表示跟单量为原始交易量的 1%
# 优先级：环境变量 (.env) > 配置文件 (bitget_config.json)
# BITGET_SCALE_RATIO=0.01

# 说明：
# 1. 模拟盘和实盘需要分别创建 API Key
# 2. 建议先使用模式 0（只在模拟盘）充分测试
# 3. 模式 2（双盘同步）可用于验证策略一致性
# 4. 白名单模型等参数请在 Web 配置页面设置（http://localhost:5010/settings）
# 5. 所有仓位都会强制设置止盈止损，若源交易缺少止盈止损将拒绝跟单
